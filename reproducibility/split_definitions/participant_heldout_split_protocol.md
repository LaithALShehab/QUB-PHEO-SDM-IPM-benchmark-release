# Participant-held-out split protocol

The benchmark follows a participant-held-out evaluation protocol. Participants are separated across training, validation, and test partitions so that test participants are not used during model fitting.

The final released result artifacts include the prediction files, metrics, classification reports, and confusion matrices generated under this protocol.

Raw QUB-PHEO data files are not redistributed in this public release.
